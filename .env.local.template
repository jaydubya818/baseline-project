# ===========================================
# E2E Testing Credentials - ROTATE IMMEDIATELY
# ===========================================
# 
# INSTRUCTIONS FOR CREDENTIAL ROTATION:
# 1. Log into https://your-app-url.com with jaydubya818a@yahoo.com
# 2. Go to Account Settings → Security → Change Password
# 3. Generate a strong password using a password manager:
#    - Minimum 32 characters
#    - Mixed case, numbers, symbols
#    - Example: Use `openssl rand -base64 32` to generate
# 4. Update the values below with the NEW credentials
# 5. NEVER commit this file to git (it's in .gitignore)
#
# After rotation, copy this file to .env and fill in your rotated credentials:

SUPER_ADMIN_EMAIL="jaydubya818a@yahoo.com"
SUPER_ADMIN_PASSWORD="YOUR-NEW-SECURE-PASSWORD-HERE"

# To generate a secure password, run:
# openssl rand -base64 32
# Or use a password manager like 1Password, LastPass, or Bitwarden
